build-test:
  stage: build
  tags: ["windows", "python"]
  script:
    - echo $CI_JOB_TOKEN | docker login -u gitlab-ci-token --password-stdin $CI_REGISTRY
    - docker build -t test-image:$CI_COMMIT_SHORT_SHA .
    - docker images | findstr test-image